import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  foldersList: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 25,
    paddingTop: 40,
    gap: 12,
  },

  createList: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 22,
  },
  
});
