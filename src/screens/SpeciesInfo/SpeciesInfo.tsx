import React from 'react';
import { Text, View } from 'react-native';
import styles from './styles';

export default function SpeciesInfoScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Species Info Screen</Text>
    </View>
  );
}
