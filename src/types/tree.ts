export type Tree = {
  tree_id: string;
  species: string;
  street_address: string;
  bank: number;
  row: number;
  health_status: string;
  planted: boolean;
  sold: boolean;
  reserved: boolean;
  reserved_for: string;
  street_ready: boolean;
  required_action: string;
  source: string;
  date: Date;
  qr_code_url: string;
};
